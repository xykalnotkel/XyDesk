# 6.8.4 — Realtime Nyata Gada Dummy

**Tanggal:** 2026-09-13
**Tipe:** fix — REALTIME

## Ringkasan — pastikan realtime dan nyata gada placeholder/dummy/mockup
- **Hub `cloudflare/src/hub.js`:** `fetch` sekarang handle HTTP `GET /stats` & `/hub/devices` — baca `this.sockets()` Hibernation langsung, return `{ onlineDevices, onlineClients, totalSockets, devices: [{id,name,since}] }` realtime, bukan hardcode 184/23.
- **AuthStore `cloudflare/src/authstore.js`:** tambah `GET /admin/users` (list `storage.list({prefix:'user:'})` nyata 9 users), `GET /admin/stats` (totalUsers/guest hitung langsung), `GET/POST /admin/maintenance` (storage `admin:maintenance` nyata).
- **Admin `cloudflare/src/admin.js`:** semua endpoint sekarang **tanpa mock** — `stats` gabung Hub+AuthStore, `users` dari AuthStore mapped role admin vs viewer, `devices` dari Hub `/hub/devices`, `maintenance` GET public (untuk web banner) + POST admin, semua cek JWT nyata. Fallback `[]`/`0` kalau kosong, bukan dummy 2483.
- **Deploy Worker 673206c4:** `curl /admin/stats` → `{totalUsers:9, onlineDevices:0, ...}` nyata, `curl /admin/users` → 9 emails real, `curl /admin/devices` → `[]` realtime (0 online), `curl /admin/maintenance` public OK.

## Artefak
- `signal.xydesk.my.id` realtime, `admin.xydesk-admin.pages.dev` otomatis nyata, `v6.8.4`.
