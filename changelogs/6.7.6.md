# Changelog 6.7.6 - 2026-09-07

> Build 41. Hotfix CI: format Dart + Rust + CHANGELOG + TURN direct kind — Build 34169492118 failed 4 jobs, now fixed.

## Ringkasan
Build 6.7.5 (6ee849d) gagal di CI karena 4 job: `Cek Lintas-Dokumen` (CHANGELOG top version tidak ada), `Analisis Statis Flutter` (dart format 3 files), `Uji Logika Host Rust` (cargo fmt 9 files), `Uji Backend Worker` (TURN direct kind tidak dikenal). Rilis 6.7.6 memperbaiki semua tanpa ubah fitur — hanya format & validasi.

## Ditambahkan
- **TURN direct kind di test** `cloudflare/test/turn-multi.test.js`: allow `direct` di `['static','direct','cloudflare','rest']` — sebelumnya hanya 3, padahal `cloudflare/src/turn.js` sudah punya provider `direct` (ExpressTurn static credential direct).

## Diperbaiki
- **CHANGELOG.md** `CHANGELOG.md`: top version parsing `^## \[([^\]]+)\]` gagal karena file lama punya `## Daftar versi (per file)` dan `## [Belum terbit] → 6.7.5 DONE` (format salah). Sekarang format Keep a Changelog benar: `## [Belum terbit]` kosong lalu `## [6.7.5]` dan sekarang `## [6.7.6]` di paling atas. `tool/check_version.py` LULUS.
- **Dart format** `lib/features/devices/device_detail_page.dart`, `session_page.dart`, `session_panels.dart`: `dart format` 3.9.2 (67 files, 3 changed) — `_RealtimePing` long lines dipecah, ternary indent, `if (ping!=null) _RealtimePing` → single line vs multi.
- **Rust format** `host/src/*.rs` 9 files: `cargo fmt` — `audio.rs` chain `.into_iter().map`, import order `IPropertyStore` vs `STGM_READ`, `PROPERTYKEY` multi-line, `if !is_null() && vt==31` break, `control.rs` error string multi-line, `dxgi.rs` match cast multi-line, `gdi.rs`, `lib.rs`, `main.rs`, `screen.rs`, `virtual_display.rs`, `virtual_mic.rs`.
- **Host fmt** sudah di-commit sebelumnya, sekarang included.

## Diubah
- `pubspec.yaml` 6.7.5+40 → 6.7.6+41
- `host/Cargo.toml` 6.7.5 → 6.7.6
- `web/package.json` 6.7.5 → 6.7.6
- `desktop/package.json` 6.7.5 → 6.7.6
- `CHANGELOG.md` top → 6.7.6
- `cloudflare/test/turn-multi.test.js` kind list + direct

## Build
- `python3 tool/check_version.py` → Lulus: versi 6.7.6+41 konsisten
- `dart format --output=none --set-exit-if-changed lib tool` → 0 changed (ok)
- `cargo fmt --check` → ok
- `node --test` cloudflare 94 tests pass (previously 93 pass 1 fail)

## Jalur CI
- Build 34169492118 failed 4 jobs → fixed in this commit, next Build should be SUCCESS → Release → Deploy Web → Deploy News → article.
