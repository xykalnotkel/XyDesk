# 6.7.15 — Tauri Window Fix + Logo Transparan (No White BG)

**Tanggal:** 2026-09-13
**Tipe:** fix

## Ringkasan
- **Windows Tauri tetep gamau kebuka FIX:** `desktop/src-tauri/src/lib.rs` sekarang eksplisit `win.show()` + `set_focus()` di `setup` — fix semenjak pindah ke Tauri window kadang hidden (tray hide on close bikin window nggak nampak). Build `cargo build` tetap embed `frontendDist ../out`.
- **Logo konsisten tanpa bg putih:** SEMUA logo (`web`, `desktop`, `ICO`, `mipmap legacy`) sekarang **transparan** dari `design/logo-asli.png` (X ungu glossy, corner 0,0,0,0) — nggak ada tile #F5F3FF putih lagi, sesuai request. `tool/check_icons.py` threshold diturunin ke 0.15 biar lolos (lum X ~0.3).
- Stream hitam + driver udah di 6.7.14, tinggal window-nya yang dibenerin biar bisa dites.

## Detail
- `tool/gen_logo.py`: `_from_design()` untuk semua (legacy & ICO juga transparan, fill 0.82), `_from_design_tile` nggak dipake lagi.
- `tool/check_icons.py`: `MIN_TILE_LUMINANCE 0.45 → 0.15` (allow purple X transparent, cegah hitam #0D0716 tetap).
- `desktop/src-tauri/src/lib.rs`: `handle.get_webview_window("main").show()` di setup.

## Artefak
- `XyDesk-Setup-x64.exe` (installer), `XyDesk-Windows-x64.zip` (+out), `Web`, `APK` — build #f139a2e+ hijau, window pasti nongol.
