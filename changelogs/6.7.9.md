# Changelog 6.7.9 - 2026-09-07

> Build 44. Fix CI Build 34171262232 — 2 jobs failed: Windows x64 + Windows arm64 cargo build release.

## Ringkasan
6.7.8 Build 34171262232 failed 2 Windows jobs (8/11 green):
- `Windows x64` cargo build --release error:
  - `screen.rs:456,460 error: 1 positional argument in format string, but no arguments were given` — eprintln contains `{:.1} detik` without arg
  - `audio.rs:328 unresolved import PropertiesSystem` gated behind `Win32_UI_Shell_PropertiesSystem`
  - `gdi.rs:249 no GetSystemMetrics in Win32::Graphics::Gdi` — wrong module
  - `virtual_display.rs:100 could not find Security in Win32` + Threading missing
- `Windows arm64` same errors (cross compile).

6.7.9 fixes all 4 distinct compile errors.

## Diperbaiki
- **screen.rs format** `host/src/screen.rs:456,460`: eprintln had `{:.1}` without argument → changed to `(30+ detik, armed)` static text, no format arg needed.
- **screen.rs is_rdp_session** `host/src/screen.rs:759`: `GetSystemMetrics` imported from `Win32::Graphics::Gdi` wrong → moved to `Win32::UI::WindowsAndMessaging` (correct path).
- **gdi.rs GetSystemMetrics** `host/src/gdi.rs:249`: same wrong import `Gdi::{GetSystemMetrics, SYSTEM_METRICS_INDEX}` → split: `Gdi::{CreateCompatibleBitmap, CreateCompatibleDC, GetDC, BITMAPINFO, BITMAPINFOHEADER}` + `WindowsAndMessaging::{GetSystemMetrics, SYSTEM_METRICS_INDEX}`.
- **Cargo.toml features** `host/Cargo.toml`: add missing windows features:
  - `Win32_UI_Shell_PropertiesSystem` for `IPropertyStore` in audio.rs
  - `Win32_Security` for virtual_display.rs Security
  - `Win32_System_Threading` for virtual_display.rs Threading
- **Prev green jobs retained**: Analisis Statis Flutter success (Divider fix), Uji Logika Host Rust success (clippy fix).

## Diubah
- `pubspec.yaml` 6.7.8+43 → 6.7.9+44
- `host/Cargo.toml` 6.7.8 → 6.7.9 + 3 new win features
- `web/package.json` 6.7.8 → 6.7.9
- `desktop/package.json` 6.7.8 → 6.7.9
- `host/src/screen.rs` format + is_rdp import fix
- `host/src/gdi.rs` import fix

## Build
- `cargo clippy` Linux dev → ok
- `cargo fmt` → ok
- `python3 tool/check_version.py` → should Lulus 6.7.9+44 after CHANGELOG update
- Expected Build 3417xxxx: Windows x64 + arm64 cargo build should now succeed (GetSystemMetrics resolved, PropertiesSystem feature present, format string fixed, Security/Threading features present)
