# 6.7.14 — Driver Offline Bundle + 1-Klik Auto-Download (No Manual)

**Tanggal:** 2026-09-11
**Tipe:** fix

## Ringkasan
- **Hitam tapi konek FIX tanpa manual:** `Pengaturan → Pasang Semua Driver (1-Klik)` sekarang **auto fallback download online** kalau bundling offline kosong (VDD & VB-CABLE). Sebelumnya kalau `install.bat`/`VBCABLE_Setup_x64.exe` nggak ada → `Berkas tidak ditemukan` → hitam terus.
- **Offline bundling dilengkapi:** `VBCABLE_Setup_x64.exe` 916K sudah masuk ke `packaging/windows/drivers/audio/` jadi installer `XyDesk-Setup-x64.exe` bawa audio driver offline. VDD (IddSampleDriver/MttVDD) tetap fallback download dari `host/driver/install.ps1` (VirtualDrivers 25.7.23) biar nggak perlu `pnputil` manual.
- Folder nggak berantakan lagi: `drivers/` di `Program Files\XyDesk` sekarang ada `VBCABLE_Setup_x64.exe`, `IddSampleDriver/install.bat` + `option.txt`, dan `audio/` lengkap.

## Detail
- `desktop/src-tauri/src/commands.rs:install_driver` — kalau `install.bat` gagal atau tidak ada, langsung `iwr https://raw.githubusercontent.com/xykalnotkel/XyDesk/main/host/driver/install.ps1` → `install.ps1` → download `Virtual-Display-Driver` + fallback. Sama untuk audio via `install-audio.ps1`.
- `packaging/windows/drivers/audio/VBCABLE_Setup_x64.exe` 916K dibundle offline (dari `download.vb-audio.com` Pack45).
- Stream test: konek → `virtual-display-driver` aktif → `framesCaptured` >0 → tidak hitam lagi di headless/RDP/lock.

## Artefak
- `XyDesk-Setup-x64.exe` / `arm64` (installer), `XyDesk-Windows-x64.zip` (+out + drivers), `XyDesk-Web`, `APK` — build #c3bfd52+ hijau.
