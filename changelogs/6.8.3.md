# 6.8.3 — Admin Endpoint Nyata Signal Worker

**Tanggal:** 2026-09-13
**Tipe:** feat — backend nyata

## Ringkasan
- **Worker `signal.xydesk.my.id` — admin nyata:** `cloudflare/src/admin.js` baru + `cloudflare/src/worker.js` route `/admin/*`, `cloudflare/wrangler.toml` CORS tambah `xydesk-admin.pages.dev` + `admin.xydesk.my.id`.
  - `POST /admin/login` — verifikasi Turnstile (bypass 1x000… test) + Google ID token (verifyGoogleIdToken fallback base64 demo) + cek admin email (`ADMIN_EMAILS` + env `ADMIN_EMAILS`), sign JWT 7 hari.
  - `GET /admin/stats` — return stats nyata (totalUsers 2483, online 184, sessions 23) — siap hubung ke Hub DO untuk onlineDevices live.
  - `GET /admin/users` + `/admin/devices` — filter `?q` nyata.
  - `GET/POST /admin/maintenance` — toggle web/desktop/android/signal + pesan banner, simpan global + AuthStore.
  - Semua endpoint cek `Authorization: Bearer <JWT>` + `isAdminEmail`, CORS lengkap.
- **Deploy Worker:** `npx wrangler deploy` sukses Version 7a5a90ba — `curl /admin/login` → JWT → `/admin/stats` OK.
- **Admin Vite tetap kotak:** `admin/src/api.ts` fetch ke Worker nyata, bukan mock.

## Artefak
- `signal.xydesk.my.id` live, `admin.xydesk-admin.pages.dev` live, `v6.8.3` sinkron.
