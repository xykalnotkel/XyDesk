# Changelog 6.7.7 - 2026-09-07

> Build 42. Fix CI Build 34170387236 — 2 jobs failed: flutter analyze (unused _connecting, _ConnectingView, missing import tokens) + host unit tests (bitrate 0 auto).

## Ringkasan
6.7.6 Build failed lagi karena:
- `Analisis Statis Flutter`: `flutter analyze --fatal-infos --fatal-warnings` — 3 issues: unused_field `_connecting`, unused_element `_ConnectingView` di `session_page.dart`, undefined_getter `c` di `splash_page.dart` (missing import `tokens.dart`).
- `Uji Logika Host Rust`: 3 failed — `aksi_video_bitrate_di_luar_batas_ditolak` expects 0 rejected padahal 0=Auto allowed (Founder request), causing PoisonError di 2 test lain `bitrate_bawaan_dan_batas_ditolak` & `bitrate_valid_disimpan`.

6.7.7 fixes all.

## Ditambahkan
- Nothing new feature, only fixes.

## Diperbaiki
- **Flutter analyze** `lib/features/session/session_page.dart`: remove `bool _connecting` field (was unused after Founder request no Loading Connection), remove `_ConnectingView` class entirely (was deprecated, unused). Comment about it kept but class removed. `dart format` 0 changed.
- **Flutter analyze** `lib/features/splash/splash_page.dart`: add `import '../../core/tokens.dart';` for `context.c` extension — was missing, causing `undefined_getter c`.
- **Host control test** `host/src/control.rs`: `aksi_video_bitrate_di_luar_batas_ditolak` — 0 Mbps now allowed as Auto (ok=true), only 999 rejected. Previously loop ["0","999"] both expected false, now 0 expects true.
- **Host screen tests** `host/src/screen.rs`: no change needed, but PoisonError from control test panic now gone — 122 passed.

## Diubah
- `pubspec.yaml` 6.7.6+41 → 6.7.7+42
- `host/Cargo.toml` 6.7.6 → 6.7.7
- `web/package.json` 6.7.6 → 6.7.7
- `desktop/package.json` 6.7.6 → 6.7.7
- `lib/features/session/session_page.dart` — remove _connecting & _ConnectingView
- `lib/features/splash/splash_page.dart` — import tokens
- `host/src/control.rs` — fix test for bitrate 0 auto

## Build
- `cargo test --lib` → 122 passed 0 failed (previously 119/3)
- `cargo fmt --check` → ok
- `dart format` → 0 changed
- `python3 tool/check_version.py` → Lulus 6.7.7+42
- `node --test` cloudflare → 94 pass
- Web build vite 295.69kB SUCCESS
