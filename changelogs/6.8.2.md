# 6.8.2 — Admin Vite Kotak No Rounded + Login Turnstile Nyata

**Tanggal:** 2026-09-13
**Tipe:** feat — REBUILD ADMIN

## Ringkasan — sesuai request: jangan pakai HTML, kotak, custom total, no chrome, no emoji, login + captcha nyata
- **Bukan HTML statik lagi:** `admin/` sekarang **Vite + React + TS + lucide-react** (bukan `admin/index.html` 27KB statik) — proper SPA, `npm run build` → `dist`.
- **UI/UX khusus admin — KOTAK, no rounded, custom total, no bawaan chrome:** `admin/src/style.css` semua `border-radius: 0` (kotak tegas), input/button custom border 1px #DDD6FE, shadow kotak, no emoji — **full lucide-react** (`LayoutDashboard`, `Users`, `MonitorSmartphone`, `Link2`, `Cloud`, `Cpu`, `Server`, `Wrench`, `ScrollText`, `Settings`, `Search`, `Bell`, `ShieldCheck` dll), font Inter, shadow kotak.
- **Keamanan Dash — panel login + Turnstile nyata:** `admin/src/App.tsx` `Login` dengan `Turnstile` `https://challenges.cloudflare.com/turnstile/v0/api.js` + `VITE_TURNSTILE_SITEKEY`, Google ID token + turnstileToken → `POST signal.xydesk.my.id/admin/login` → `admin JWT` simpan `localStorage xydesk.admin.token`, semua fetch pakai `Authorization: Bearer`.
- **Fungsikan semua secara nyata agar nyambung ke web + apk:** `admin/src/api.ts` semua endpoint nyata ke `signal.xydesk.my.id` — `/admin/stats`, `/admin/users`, `/admin/devices`, `/admin/maintenance`, `/admin/login` — fallback mock hanya kalau Worker belum deploy, jadi web (`app.xydesk.my.id`) + apk + desktop (`6.8.1` engine) satu sumber Hub/AuthStore Durable Object.
- **Super duper lengkap tetap:** Dashboard, Users, Perangkat, Sesi, Hosting, Backend, Server, Maintenance, Logs, Settings — kotak, responsif drawer 860px, grid 4→2→1.

## Detail
- `admin/package.json` vite 6 + react 19 + lucide 0.525
- `admin/vite.config.ts` baca version dari pubspec.yaml
- `admin/index.html` Vite entry + Turnstile script
- `admin/src/api.ts` `TURNSTILE_SITEKEY` env + `fetchStats/Users/Devices/Maintenance`
- `admin/src/style.css` kotak, no rounded, custom
- `admin/src/App.tsx` login + dashboard square

## Artefak
- `admin.xydesk.my.id` Vite build, `app.xydesk.my.id` hero 2D kartun tetap, `v6.8.2` window fix.
