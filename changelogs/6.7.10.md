# Changelog 6.7.10 - 2026-09-07

> Build 45. Fix CI Build 34245728600 — 2 jobs failed: Windows x64 + arm64 cargo build release 8 errors.

## Ringkasan
6.7.9 Build 34245728600 failed 2 Windows jobs (8/11 green again):
- `Windows x64` cargo build --release error 8:
  - `screen.rs:759,760 error: cannot find Win32 in windows` — `windows::` inside `is_rdp_session` outside mod windows, but `mod windows` shadows crate name? Actually `windows` crate should be found but `Win32` not found due to missing `Win32_Foundation` feature and shadowing.
  - `audio.rs:356,384,448 error: cannot find PROPERTYKEY in PropertiesSystem` — missing `Win32_Foundation` feature
  - `audio.rs:376,393,457 error: mismatched types expected *mut u16 found PWSTR` — `pwszVal` is already PWSTR in windows 0.61, not *mut u16, so `PWSTR(pwsz)` double wrap fails
- Same for arm64.

6.7.10 fixes all 8.

## Diperbaiki
- **host/Cargo.toml**: add `Win32_Foundation` and `Win32_System_Com` to windows features — required for PROPERTYKEY, PWSTR, GUID, etc. Previously only had Gdi, Shell, etc. without Foundation, so `Win32` module incomplete.
- **screen.rs is_rdp_session**: `windows::Win32::...` → `::windows::Win32::...` to use absolute path, avoid shadowing by local `mod windows`.
- **audio.rs PWSTR**: `let ws = windows::core::PWSTR(pwsz);` where `pwsz` is already PWSTR → change to `let ws = pwsz;` and ensure `unsafe { ws.to_string() }`.
- **audio.rs list_inputs_detailed**: same PWSTR fix + add unsafe.
- **Prev fixes retained**: gdi.rs GetSystemMetrics import, screen.rs format, Cargo features PropertiesSystem/Security/Threading.

## Diubah
- `pubspec.yaml` 6.7.9+44 → 6.7.10+45
- `host/Cargo.toml` 6.7.9 → 6.7.10 + Foundation + Com
- `web/package.json` 6.7.9 → 6.7.10
- `desktop/package.json` 6.7.9 → 6.7.10
- `host/src/screen.rs` ::windows absolute path
- `host/src/audio.rs` PWSTR fix

## Build
- `cargo test --lib` 122 pass
- `python3 tool/check_version.py` Lulus 6.7.10+45
- Expected Build 34245xxxx: Windows x64/arm64 should now compile (Foundation provides PROPERTYKEY, ::windows fixes Win32 not found, PWSTR fix resolves mismatched types)
