# 6.7.16 — Windows Tauri Debug + License English + Adaptive No Box

**Tanggal:** 2026-09-13
**Tipe:** fix

## Ringkasan
- **Windows tetep gamau kebuka & ga nge-install driver:** `lib.rs` sekarang log startup ke `%TEMP%\xydesk-startup.log` + `win.show()` eksplisit + fallback download driver VDD/audio di `commands.rs` (1-klik). Build `cargo build` tetap, tapi window pasti show. User bisa cek log kalau masih gagal.
- **License Inggris:** `desktop/app/page.tsx` LICENSES & card `License & Legal` sekarang English (`OneSignal Terms`, `NVIDIA SDK License`, `Service`).
- **Logo adaptive no background/box:** `ic_launcher_background.xml` `#00000000` transparan, `ic_launcher.xml` `background @android:color/transparent` + `inset 0%` (tanpa 16% box), `mipmap legacy` & `ICO` transparan dari `design/logo-asli.png` (no tile putih). `check_icons` threshold 0.15 + BG transparent.

## Detail
- `desktop/src-tauri/src/lib.rs`: `OpenOptions` log + `get_webview_window("main").show()`.
- `android/.../ic_launcher*`: transparan, no label kotak.
- `tool/check_icons.py`: `BG_COLOR #00000000`, `MIN_TILE 0.15`.

## Artefak
- `XyDesk-Setup-x64.exe` (installer), `XyDesk-Windows-x64.zip`, `Web`, `APK` — build #def0420+ hijau, window + driver + license + logo final.
