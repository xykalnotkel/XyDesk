<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Izin yang benar-benar dipakai aplikasi. Izin fitur masa depan tidak
         dideklarasikan sebelum implementasinya ada agar permukaan serangan,
         Data Safety Form, dan proses review Play Store tetap minimum. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Pindai QR host (mobile_scanner). Kamera tidak dipakai untuk hal lain. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Mic perangkat dikirim ke host selama sesi (rtc_service.getUserMedia,
         toggle "Mic" di halaman sesi). Tanpa deklarasi ini Android menolak di
         tingkat OS: getUserMedia gagal apa pun yang tertulis di UI, dan halaman
         Izin hanya bisa menebak statusnya dari enumerateDevices. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Rute/volume audio selama sesi WebRTC (dipakai flutter_webrtc untuk
         memilih perangkat keluaran: speaker, earpiece, headset). -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- Belum dideklarasikan, menunggu implementasinya ada (lihat komentar di
         atas): BLUETOOTH_CONNECT (rute headset BT di Android 12+) dan
         FOREGROUND_SERVICE_MEDIA_PROJECTION (host mode Android). -->
    <!-- Tetap memerlukan konfirmasi pengguna melalui installer Android. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <!-- Gamepad opsional; deklarasi feature tidak meminta izin runtime. -->
    <uses-feature android:name="android.hardware.gamepad" android:required="false"/>
    <!-- Picture-in-Picture untuk floating window saat sesi aktif. -->
    <uses-feature android:name="android.software.picture_in_picture" android:required="false"/>

    <application
        android:requestLegacyExternalStorage="false"
        android:label="XyDesk"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:supportsPictureInPicture="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode|screenSize|smallestScreenSize"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Hanya APK yang sudah lolos verifikasi native yang dibagikan ke installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updateprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_file_paths" />
        </provider>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Agar url_launcher bisa membuka tautan https di Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Coba buka aplikasi sosial langsung; fallback tetap memakai URL https. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="snssdk1233" />
        </intent>
    </queries>
</manifest>
