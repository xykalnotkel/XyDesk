name: xydesk
description: "XyDesk — remote desktop low-latency untuk gaming dan kerja."
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  shared_preferences: ^2.3.2
  http: ^1.6.0
  google_sign_in: ^7.2.0
  flutter_secure_storage: ^11.0.0
  # Ikon Lucide — stroke tipis konsisten, sesuai mockup.
  # Material Icons terlalu tebal/filled dan membuat UI terasa berbeda.
  lucide_icons_flutter: ^3.1.15
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/img/
    - assets/libraryicons/

  # Inter di-bundle langsung (bukan diunduh runtime) supaya tampilan
  # pasti sama di semua perangkat, termasuk tanpa internet.
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
          weight: 400
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
